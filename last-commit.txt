V="0.0.65"
\. "$HOME/.nvm/nvm.sh"
npm version "$V" --no-git-tag-version
git add .
git commit -m "version $V: update build name without version;"
git tag -d "$V" 2>/dev/null
git tag -a "$V" -m "version $V: update build name without version;"
git push origin main -f
git push origin --tags -f
